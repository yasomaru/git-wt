linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - gofmt
    - goimports

linters-settings:
  errcheck:
    check-type-assertions: true

run:
  timeout: 5m
